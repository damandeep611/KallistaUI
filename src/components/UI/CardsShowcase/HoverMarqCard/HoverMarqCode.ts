export const HoverMarqCode =
  'import { useState } from "react";\r\nimport { motion } from "framer-motion";\r\n\r\ninterface HoverMarqProps {\r\n  heading: string;\r\n  backgroundText: string;\r\n}\r\n\r\nexport default function HoverMarq({ heading, backgroundText }: HoverMarqProps) {\r\n  const [isHovered, setIsHovered] = useState(false);\r\n  return (\r\n    <motion.div\r\n      className="relative w-64 h-96 rounded-lg overflow-hidden cursor-pointer"\r\n      onHoverStart={() => setIsHovered(true)}\r\n      onHoverEnd={() => setIsHovered(false)}\r\n    >\r\n      {/* gradient bg */}\r\n      <motion.div\r\n        className="absolute inset-0 bg-gradient-to-br from-blue-700 to to-violet-700"\r\n        initial={{ y: "100%" }}\r\n        animate={{ y: 0 }}\r\n        transition={{ duration: 0.5 }}\r\n      />\r\n      {/* heading text */}\r\n      <motion.div className="relative z-10 text-white text-3xl font-bold p-6">\r\n        {heading}\r\n      </motion.div>\r\n      {/* marquee bg text */}\r\n      <motion.div>\r\n        <motion.div animate={{ x: isHovered ? [0, -1000] : 0 }}>\r\n          {backgroundText.repeat(10)}\r\n        </motion.div>\r\n      </motion.div>\r\n    </motion.div>\r\n  );\r\n}\r\n';
